# Build output (generated by `pnpm build`; regenerated for the file:/link: consumer and on publish)
dist/
# Node
node_modules/
npm-debug.log*
# Editor / OS
.DS_Store
.vscode/
.remember/
# Planning docs (local only)
docs/
# Live discovery scripts: they hold account numbers and run against real tenants.
spikes/
# Secrets and local-only context — never committed or published
.dev.vars
.env
CLAUDE.md
leak-patterns.txt
*.tgz
ROADMAP.md
